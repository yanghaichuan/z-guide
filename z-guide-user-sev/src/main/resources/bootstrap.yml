spring:
  cloud:
    config:
      name: application-zguide
      profile: test
      label: master
      uri: http://localhost:20002
    bus:
      trace:
        enabled: true
    stream:
      kafka:
        binder:
          brokers: localhost:9092